<link href="/stylesheets/index.css" rel="stylesheet">
<container>
    <div class="row">
        <div class="">
            {{#each artwork}}
            <div class="container">
                    <img src="{{art_source}}" class="image w-100 shadow-1-strong rounded mb-4" alt="">
                        <a href="/artwork/{{art_id}}">
                        <div class="overlay">
                            <div class="text">{{art_title}}</div>
                            <div class="description-text">{{art_creator}}</div>
                        </div>
                        </a>
                    </div>
            {{/each}}
        </div>
    </div>
</container>